# -----------------------------------------------------------------------------
# Gentrobyte Internship Portal - Spring Boot Backend (OTP Verification)
# -----------------------------------------------------------------------------
# MongoDB configuration (Local MongoDB, database will be created on first write)
# Only use GENTROBYTE_db
spring.data.mongodb.uri=mongodb://localhost:27017/GENTROBYTE_db
spring.data.mongodb.database=GENTROBYTE_db

# -----------------------------------------------------------------------------
# Email (SMTP) configuration
# For Gmail: enable 2-Step Verification and generate an App Password:
# https://support.google.com/accounts/answer/185833
# Then set the 16-digit App Password below.
# -----------------------------------------------------------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=YOUR_EMAIL@gmail.com
spring.mail.password=YOUR_16_DIGIT_APP_PASSWORD

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# MongoDB Atlas connection string
spring.data.mongodb.uri=mongodb+srv://727724eucy023_db_user:Wa2krfKDkDISSSi4@cluster0.fumn7uj.mongodb.net/?appName=Cluster0

# JWT settings for API authentication
security.jwt.secret=REPLACE_WITH_A_SECRET_AT_LEAST_32_CHARACTERS_LONG
security.jwt.expiration-ms=3600000

# -----------------------------------------------------------------------------
# Alternate SMTP providers (SendGrid/Mailgun) using API keys
# Use the provider's SMTP host/port and set the API key as the password.
# Example (SendGrid):
# spring.mail.host=smtp.sendgrid.net
# spring.mail.port=587
# spring.mail.username=apikey
# spring.mail.password=YOUR_SENDGRID_API_KEY
# -----------------------------------------------------------------------------

# Server port (optional)
# If 8080 is already in use on your machine, change this value.
server.port=8081
